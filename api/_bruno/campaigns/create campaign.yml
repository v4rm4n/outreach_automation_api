info:
  name: create campaign
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/campaigns"
  body:
    type: json
    data: |-
      {
        "name": "Lakme Collab 2026",
        "template_id": "6a2be7ee4359afd3c8fa79b6",
        "priority": 3
      }
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
