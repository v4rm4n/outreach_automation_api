info:
  name: create campaign
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/campaigns"
  body:
    type: json
    data: |-
      {
        "name": "Lakme Collab 2026",
        "template_id": "6a2ad9fb8ee5f5c55327704a",
        "priority": 3
      }
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
