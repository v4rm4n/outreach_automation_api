info:
  name: add creator to campaign
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/campaigns/:campaign_id/creators"
  params:
    - name: campaign_id
      value: 6a2bb1cf99f551fd08547ca4
      type: path
  body:
    type: json
    data: |-
      {
        "creator_ids": [
          "6a2bb1c3016ed217a7399fc0"
        ],
        "scheduled_for": "2026-06-12T12:46:00+05:30"
      }
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
