info:
  name: add creator
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/creators"
  body:
    type: json
    data: |-
      {
        "handle": "glossy_priya",
        "platform": "instagram"
      }
  auth:
    type: bearer
    token: "{{auth_tok}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
