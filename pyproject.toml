[project]
name = "outreach-automation-api"
version = "0.1.0"
description = "Run campaigns for automating template messages for social media platforms."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aio-pika>=9.6.2",
    "dotenv>=0.9.9",
    "fastapi>=0.136.3",
    "motor>=3.7.1",
    "redis>=8.0.0",
    "structlog>=26.1.0",
    "uvicorn>=0.49.0",
]
